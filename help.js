// Commmand tokens
const HELP_STR = "test help"

/*
* Check the status of a <service>.
* @return
*/

getHelpStr = function(service) {
  return HELP_STR
};

processCommand = function(tokens) {

}

module.exports = {
    getHelpStr: getHelpStr,
    processCommand: processCommand
};
